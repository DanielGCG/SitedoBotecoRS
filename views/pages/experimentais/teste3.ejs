<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Teste de Áudio</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            margin: 50px;
        }

        button {
            padding: 10px 20px;
            font-size: 16px;
            cursor: pointer;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 5px;
        }

        button:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
    <h1>Teste de Reprodução de Áudio</h1>
    <p>Clique no botão abaixo para habilitar o áudio. Após isso, o som será reproduzido automaticamente sem interação.</p>
    <button id="enableAudioButton">Habilitar Áudio</button>

    <script>
        let isAudioEnabled = false;

        function enableAudio() {
            isAudioEnabled = true;
            console.log("Áudio habilitado! Agora todos os sons podem ser reproduzidos.");
            
            // Tocar um som automaticamente após o áudio ser habilitado
            setTimeout(() => {
                if (isAudioEnabled) {
                    console.log("Tentando tocar um som automaticamente sem interação do usuário...");
                    playAudio('/pages/minigames/quadrados/sounds/play2.mp3', 0.5);
                }
            }, 2000); // 2 segundos após o áudio ser habilitado
        }

        document.getElementById('enableAudioButton').addEventListener('click', () => {
            // Tocar o som 'death.mp3' para desbloquear o áudio
            playAudio('/pages/minigames/quadrados/sounds/death.mp3', 0.5);
            enableAudio(); // Habilitar o estado de áudio
        });

        // Função genérica para reproduzir áudio
        function playAudio(soundPath, volume = 1.0) {
            if (!isAudioEnabled) {
                console.error("Áudio ainda não foi habilitado pelo usuário.");
                return;
            }

            const audio = new Audio(soundPath);
            audio.volume = volume;
            audio.play().then(() => {
                console.log(`Som reproduzido com sucesso: ${soundPath}`);
            }).catch(error => {
                console.error("Erro ao tentar reproduzir o som:", error);
            });
        }
    </script>
</body>
</html>
